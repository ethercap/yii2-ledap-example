ledap.App.register(["form-item", "group", "tab", 'grid', 'pager'], Vue);
const app = new Vue({
    el: "#app",
    data : {
        'colors' : data.colors,
        'saleData' : data.saleData,
    },
});

